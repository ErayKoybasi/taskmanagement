const config = {
    db : {
        host : "localhost",
        user : "root",
        password : "<dbpass>",
        database : "todoapp"
    },
    email : {
        username : "<mailaddress>",
        password : "<mailpass>",
        from : "<mailaddress>"
    }
}

module.exports = config;